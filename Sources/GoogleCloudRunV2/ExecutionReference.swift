// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
import GoogleCloudWkt

/// Reference to an Execution. Use /Executions.GetExecution with the given name
/// to get full execution including the latest status.
public struct ExecutionReference: Codable, Equatable, GoogleCloudWkt._AnyPackable,
  Sendable
{
  /// Name of the execution.
  public var name: Swift.String = Swift.String()

  /// Creation timestamp of the execution.
  public var createTime: GoogleCloudWkt.Timestamp? = nil

  /// Creation timestamp of the execution.
  public var completionTime: GoogleCloudWkt.Timestamp? = nil

  /// The deletion time of the execution. It is only
  /// populated as a response to a Delete request.
  public var deleteTime: GoogleCloudWkt.Timestamp? = nil

  /// Status for the execution completion.
  public var completionStatus: ExecutionReference.CompletionStatus =
    ExecutionReference.CompletionStatus()

  /// Initialize a new instance of `ExecutionReference`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = ExecutionReference().with { $0.name = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  /// Possible execution completion status.
  public enum CompletionStatus: Codable, Equatable, Sendable {
    case unspecified
    case executionSucceeded
    case executionFailed
    case executionRunning
    case executionPending
    case executionCancelled
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .executionSucceeded: return 1
      case .executionFailed: return 2
      case .executionRunning: return 3
      case .executionPending: return 4
      case .executionCancelled: return 5
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: String? {
      switch self {
      case .unspecified: return "COMPLETION_STATUS_UNSPECIFIED"
      case .executionSucceeded: return "EXECUTION_SUCCEEDED"
      case .executionFailed: return "EXECUTION_FAILED"
      case .executionRunning: return "EXECUTION_RUNNING"
      case .executionPending: return "EXECUTION_PENDING"
      case .executionCancelled: return "EXECUTION_CANCELLED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to ``.unknownStringValue(_:)``.
    public init(stringValue: String) {
      switch stringValue {
      case "COMPLETION_STATUS_UNSPECIFIED": self = .unspecified
      case "EXECUTION_SUCCEEDED": self = .executionSucceeded
      case "EXECUTION_FAILED": self = .executionFailed
      case "EXECUTION_RUNNING": self = .executionRunning
      case "EXECUTION_PENDING": self = .executionPending
      case "EXECUTION_CANCELLED": self = .executionCancelled
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to ``.unknownIntValue(_:)``.
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .executionSucceeded
      case 2: self = .executionFailed
      case 3: self = .executionRunning
      case 4: self = .executionPending
      case 5: self = .executionCancelled
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .executionSucceeded: return try container.encode(1)
      case .executionFailed: return try container.encode(2)
      case .executionRunning: return try container.encode(3)
      case .executionPending: return try container.encode(4)
      case .executionCancelled: return try container.encode(5)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: String {
    return "type.googleapis.com/google.cloud.run.v2.ExecutionReference"
  }
  public init(fromAny any: GoogleCloudWkt.`Any`) throws {
    self = try GoogleCloudWkt._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWkt.Struct {
    return try GoogleCloudWkt._slowAnySerialize(message: self)
  }
}
